# awol2005ex3-kerberos-asn1

Kerberos 协议 ASN.1 类型定义。包含 `KrbCred`、`Ticket`、`PrincipalName`、`EncryptionKey`、
`KerberosTime`、`TicketFlags` 等核心类型的 Rust 结构体和 ASN.1 DER 序列化/反序列化。

基于 `red-asn1` / `red-asn1-derive` 实现。

## Crates.io

`awol2005ex3-kerberos-asn1`
