# awol2005ex3-kerberos-constants

Kerberos 协议常量定义，包括：
- `etypes`：加密类型编号（AES256、AES128、RC4 等）
- `address_types`：地址类型
- `pa_data_types`：PA-DATA 类型
- `principal_names`：Principal name 类型
- `ticket_flags`：票据标志位
- `key_usages`：加密 Key Usage 值

## Crates.io

`awol2005ex3-kerberos-constants`
